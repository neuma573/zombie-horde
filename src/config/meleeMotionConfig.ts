// Shared contact timing keeps damage and the rendered swing on the same timeline.
export const MELEE_MOTION = {
  durationMs: 360,
  windupProgress: 0.28,
  impactProgress: 0.42,
  followThroughProgress: 0.64,
  upperArmLength: 12,
  forearmLength: 16,
} as const;
